# Bug 练习提示 - 数据处理器

本文档列出了 `data_processor_*.py` 中所有可改进的问题点，但**不提供答案**。
请使用这些提示进行调试、测试和代码重构练习。

---

## 🔴 严重问题 (Critical)

### 1. 资源泄露
- **位置**: `process_file_lines()` 函数
- **提示**: 文件打开后没有正确关闭，即使发生异常也不会关闭
- **影响**: 文件描述符泄露，可能导致 "too many open files" 错误

### 2. 可变默认参数陷阱
- **位置**: `load_config()` 函数
- **提示**: 默认参数 `cache={}` 是可变对象
- **影响**: 多次调用函数会共享同一个字典，导致意外行为

### 3. 除零错误
- **位置**: `calculate_average()` 函数
- **提示**: 当传入空列表时会发生什么？
- **影响**: `ZeroDivisionError`

### 4. 索引越界
- **位置**: `find_max_value()` 函数
- **提示**: 当传入空列表时，第一行代码会发生什么？
- **影响**: `IndexError`

---

## 🟠 中等问题 (Major)

### 5. 缺少异常处理
- **位置**: `process_file_lines()` 函数
- **提示**: 
  - 文件不存在时会怎样？
  - 行格式不正确时会怎样？
  - 转换整数失败时会怎样？
- **影响**: 程序崩溃，没有友好的错误信息

### 6. 边界条件错误
- **位置**: `process_batch()` 函数
- **提示**: 当列表长度不是 batch_size 的整数倍时，最后一批数据会怎样？
- **影响**: 数据丢失

### 7. 输入验证缺失
- **位置**: `calculate_discount()` 函数
- **提示**: 
  - 折扣百分比为负数时会怎样？
  - 折扣百分比超过 100 时会怎样？
  - 价格为负数时会怎样？
- **影响**: 产生不合理的计算结果

### 8. 浅拷贝问题
- **位置**: `copy_list()` 函数
- **提示**: 如果列表包含嵌套列表，修改拷贝后的列表会影响原列表吗？
- **影响**: 意外的数据修改

### 9. 缺少键存在检查
- **位置**: `get_nested_value()` 函数
- **提示**: 如果某个 key 不存在会发生什么？
- **影响**: `KeyError` 异常

---

## 🟡 轻微问题 (Minor)

### 10. 代码重复 (DRY 原则)
- **位置**: `merge_lists()` 函数
- **提示**: 两段几乎相同的代码，能否提取为辅助函数或使用更简洁的方法？
- **影响**: 代码维护困难

### 11. 性能问题 - 列表查找
- **位置**: `merge_lists()` 函数
- **提示**: `if item not in result` 的时间复杂度是多少？对于大列表会怎样？
- **影响**: O(n²) 时间复杂度

### 12. 性能问题 - 线性搜索
- **位置**: `search_item()` 函数
- **提示**: 如果这个函数被频繁调用，有什么优化方案？
- **影响**: 每次搜索都是 O(n)

### 13. 浮点数精度问题
- **位置**: `format_currency()` 函数
- **提示**: 尝试 `format_currency(19.999)` 或涉及浮点运算的结果
- **影响**: 显示金额可能不准确

### 14. 不完整的邮箱验证
- **位置**: `validate_email()` 函数
- **提示**: 哪些无效邮箱会被错误地接受？
- **影响**: 接受格式错误的邮箱地址

### 15. 潜在的安全问题
- **位置**: `read_user_data()` 函数
- **提示**: 如果 `user_id` 来自用户输入，直接拼接到查询字符串有什么问题？
- **影响**: SQL 注入风险（虽然这里只是演示）

---

## 🧪 测试练习建议

### 边界条件测试
1. 空列表输入
2. 单元素列表
3. 极大/极小数值
4. None 值输入
5. 错误类型输入

### 异常场景测试
1. 文件不存在
2. 文件格式错误
3. 网络/IO 失败
4. 权限不足

### 性能测试
1. 大数据集（10万+ 元素）
2. 高频调用场景
3. 内存使用分析

---

## 📋 重构目标

完成以下改进后，代码应该：
- [ ] 所有资源正确关闭（使用 context manager）
- [ ] 所有边界条件有适当处理
- [ ] 所有外部输入有验证
- [ ] 消除代码重复
- [ ] 关键函数有单元测试
- [ ] 添加适当的日志记录
- [ ] 添加类型注解
- [ ] 添加文档字符串说明参数和返回值

---

## 💡 额外挑战

1. **添加单元测试**: 为每个函数编写 pytest 测试用例，覆盖正常和异常情况
2. **类型注解**: 为所有函数添加完整的类型注解
3. **日志记录**: 添加 logging 模块记录关键操作和错误
4. **配置驱动**: 将魔法数字（如 batch_size）改为可配置
5. **性能优化**: 使用 profiling 工具找出瓶颈并优化

---

**祝练习愉快！🎯**
